"""Fallback обработчик - больше не используется, но оставлен для совместимости"""
from aiogram import Router

router = Router()

# Fallback больше не нужен, так как все сообщения обрабатываются в order.py
